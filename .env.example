# Server port (default: 8080)
PORT=8080

# Sliding window size in segments (default: 6)
SLIDING_WINDOW_SIZE=6

# Log level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# Log format: json or text (default: json)
LOG_FORMAT=json
